# Lark 机器人配置

1. 在飞书开放平台创建企业自建应用，并启用机器人能力。
2. 为应用开通发送消息和上传图片所需权限，并发布应用版本。
3. 将机器人加入需要接收报表的群。
4. 复制 `lark_bot_config.example.json` 为 `lark_config.json`。
5. 填写 `app_id`、`app_secret`，以及每张图片对应群的 `chat_id`。

应用仅发送 `send_as: "app"` 的消息，不使用个人账号授权。

## 企业微信自动收件

1. 安装依赖：`python -m pip install -r requirements.txt`。
2. 重新运行 `build_windows_app.bat`。
3. 打开程序后点击“收件设置”，填写准确的 WISEWAY 和 Auslink 顺友会话名称。
4. 日常手动打开 `iMile x WISEWAY` 主群；仅有顺友货时打开 `iMile x Auslink TEMU handover` 备用群，并让待处理的完整数据消息显示在当前画面。
5. 在 Lark 桌面端打开邮箱收件箱，让“搜索邮件”输入框显示。
6. 在默认浏览器登录 iMile DC 系统。
7. 点击“自动收件”；未登录或页面不正确时程序会提示并停止。

WISEWAY 群分别提取 `Temu:` 和 `Cainiao:` 区段中每个主单号中划线后的 8 位号码。TEMU 文件先在企业微信本地缓存中匹配；缓存缺失时，程序只有在 OCR 确认当前群名和“聊天记录”页面后，才会逐个搜索并点击对应文件，下载后复制到 `input/TEMU/日期/`。Cainiao 号码会在 OCR 确认的 Lark 邮箱收件箱中逐个搜索，打开对应邮件，通过附件右键菜单中的“下载”操作保存 Excel 文件，再归档到 `input/CAINIAO/日期/`。

自动收件只处理当天发送的群消息。请让发送者、发送时间和完整数据消息同时显示；若画面中只有昨天或更早日期，程序会停止并提示，不会重复下载旧消息中的单号。

Auslink 顺友群会提取当前画面的主单号，在 Lark 邮箱中只下载 `IMILE末端预报` Excel，排除 MAWB PDF，并将通用附件名改为 `顺友{8位单号}.xlsx` 后归档到 `input/SHUNYOU/日期/`。程序使用 Windows 当前用户真实的“下载”系统目录，并为每票建立独立临时目录，避免同名文件覆盖。点击位置根据当前窗口截图动态计算，支持窗口化、全屏和不同 Windows 缩放比例，不依赖固定屏幕坐标。收件完成后会显示票数和单号，用户确认后才继续生成查询单号。

确认后程序会把 `output/query_list.txt` 提交到 iMile DC 的“中心运单查询”，等待导出任务完成并下载到项目根目录的 `中心运单查询.xlsx`。页面优先使用固定网址，没有收藏时会通过左侧搜索入口按名称进入；中文和英文页面都支持，英文导出列名会自动转换成统计所需的中文标准列名。浏览器窗口大小、分辨率和缩放比例不会影响控件定位。DC 未登录会提示用户登录并暂停，之后可点击“③ 重试中心运单导出”继续。程序不会切换或搜索群聊，不会操作聊天输入框，也不会进入写邮件界面；任何页面校验失败都会立即停止。
