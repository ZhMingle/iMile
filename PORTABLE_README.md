# iMile 日报助手便携版

这是公司内部使用的零配置便携版。解压完整 ZIP 后，双击
`iMileReportAssistant.exe` 即可运行；不要只复制 EXE，`_internal` 文件夹必须与它放在一起。

## 日报与文字发送

- 选择中心运单文件后，可在界面内生成并通过 Webhook 发送日报。
- 文字消息也只使用已经内置的 Webhook 目标。
- 使用这两项功能不需要安装 Python，也不需要登录飞书桌面端。
- `input` 和 `output` 是程序的工作目录，请保留。

## 其他功能

自动收件、DC 下载等依赖本机上的企业微信、浏览器登录状态或本机配置，不属于零配置发送能力。
便携包只包含 `wecom_download_config.example.json` 示例，不包含构建电脑上的真实企业微信配置。

## 安全提醒

便携版内置了仅用于日报发送的飞书应用凭据和 Webhook 目标，因此只能发给公司内部可信同事。
请勿上传网盘公开链接、代码仓库或群公告。电脑遗失或压缩包误发后，应立即停用相关 Webhook，
并轮换飞书应用密钥后重新构建便携版。

发布配置不会包含个人授权令牌、个人账号发送目标、发送日志或令牌缓存。
